import { Code, Monitor, Database, Wrench, Globe, Layers } from "lucide-react";

export const categoryIcons = {
  frontend: Monitor,
  backend: Code,
  fullstack: Layers,
  databases: Database,
  languages: Globe,
  tools: Wrench,
};
