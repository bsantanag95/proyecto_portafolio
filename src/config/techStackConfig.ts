import { Code, Layout, Database, Wrench, Server, Layers2 } from "lucide-react";

export const categoryIcons = {
  frontend: Layout,
  backend: Server,
  fullstack: Layers2,
  databases: Database,
  languages: Code,
  tools: Wrench,
};
